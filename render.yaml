services:
  - type: web
    name: chosanta-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm run server
    envVars:
      - key: NODE_VERSION
        value: 20.12.0
      - key: SESSION_SECRET
        generateValue: true
